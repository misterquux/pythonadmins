# Tool Projekt

## Projektbeschreibung
ein kleiner Web-Scaper

## Installationshinweis

```bash
git clone tool
cd tool
python -m venv .venv
pip install -r requirements.txt
```

## Nutzung
```bash
python main.py --url google.de  --force
```